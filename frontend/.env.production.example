# Build production : copier en .env.production avant `npm run build`
# ou définir ces variables sur la CI (Vercel / Netlify / etc.)

# URL publique du site (liens QR, redirections)
VITE_PUBLIC_APP_URL=https://votre-frontend.example.com

# URL de l’API (sans slash final)
VITE_API_URL=https://votre-api.example.com
